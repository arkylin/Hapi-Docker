# 必填：Hub 的认证令牌，CLI 连接时需要
CLI_API_TOKEN=your-secure-token-here

# 可选：Hub 的公网地址（用于 Telegram 等回调）
HAPI_PUBLIC_URL=http://your-server-ip:3006

# 可选：CORS 允许的域名（逗号分隔），默认 *。反向代理场景下 WebSocket 终端需要设为 *
CORS_ORIGINS=*

# 可选：时区
TZ=Asia/Shanghai
